<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inventory.dao.ShelfDao">

    <select id="findById" resultType="com.inventory.model.store.Shelf">
        SELECT * FROM shelf WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.inventory.model.store.Shelf">
        SELECT * FROM shelf ORDER BY id DESC
    </select>

    <insert id="insert" parameterType="com.inventory.model.store.Shelf" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shelf (name, max_weight, min_threshold, current_weight)
        VALUES (#{name}, #{maxWeight}, #{minThreshold}, #{currentWeight})
    </insert>

    <update id="update" parameterType="com.inventory.model.store.Shelf">
        UPDATE shelf
        SET name = #{name},
            max_weight = #{maxWeight},
            min_threshold = #{minThreshold}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM shelf WHERE id = #{id}
    </delete>

    <update id="updateCurrentWeight">
        UPDATE shelf
        SET current_weight = #{currentWeight}
        WHERE id = #{shelfId}
    </update>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM shelf
    </select>

</mapper>
